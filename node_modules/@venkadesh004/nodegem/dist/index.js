"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  NodeGem: () => NodeGem
});
module.exports = __toCommonJS(src_exports);

// src/functions.ts
var import_generative_ai = require("@google/generative-ai");
var import_fs = __toESM(require("fs"));
var import_path = __toESM(require("path"));
var import_googleapis = require("googleapis");
var import_local_auth = require("@google-cloud/local-auth");
var import_translate = require("@google-cloud/translate");
var import_axios = __toESM(require("axios"));
var nodemailer = require("nodemailer");
var NodeGem = class {
  API_KEY;
  modelName;
  currentModel;
  maxOutputTokens = 100;
  stopSequence = ["red"];
  temperature = 0.9;
  topP = 0.1;
  topK = 16;
  safetySettings;
  jwtClient;
  authConnect;
  gmail;
  translate = new import_translate.v2.Translate();
  bloggerAPIKey;
  constructor(API_KEY, modelName) {
    this.API_KEY = API_KEY;
    this.modelName = modelName;
    this.currentModel = new import_generative_ai.GoogleGenerativeAI(this.API_KEY).getGenerativeModel({ model: this.modelName });
  }
  async generateContent(prompt, stream) {
    if (this.modelName !== "gemini-pro") {
      return "Use Gemini pro";
    }
    if (stream) {
      const result2 = await this.currentModel.generateContentStream(prompt);
      const response2 = await result2.response;
      return response2.text();
    }
    const result = await this.currentModel.generateContent(prompt);
    const response = await result.response;
    return response.text();
  }
  switchModel(modelName) {
    this.modelName = modelName;
    this.currentModel = new import_generative_ai.GoogleGenerativeAI(this.API_KEY).getGenerativeModel({ model: this.modelName });
  }
  fileToGenerativePart(path2, mimeType) {
    return {
      inlineData: {
        data: Buffer.from(import_fs.default.readFileSync(path2)).toString("base64"),
        mimeType
      }
    };
  }
  async useTextAndImage(imageParts, stream, prompt) {
    if (this.modelName === "gemini-pro") {
      return "use Gemini pro Vision";
    }
    var newImageParts = [];
    imageParts.forEach((element) => {
      newImageParts.push(this.fileToGenerativePart(element[0], element[1]));
    });
    if (stream) {
      const result2 = await this.currentModel.generateContentStream([prompt, ...newImageParts]);
      const response2 = await result2.response;
      return response2.text();
    }
    const result = await this.currentModel.generateContent([prompt, ...newImageParts]);
    const response = await result.response;
    return response.text();
  }
  changeConfig({ maxOutputTokens, temperature, topP, topK, stopSequence }) {
    this.maxOutputTokens = maxOutputTokens;
    this.temperature = temperature;
    this.topK = topK;
    this.topP = topP;
    this.stopSequence = stopSequence;
    const generationConfig = {
      maxOuptutTokens: this.maxOutputTokens,
      stopSequences: this.stopSequence,
      temperature: this.temperature,
      topK: this.topK,
      topP: this.topP
    };
    this.currentModel = new import_generative_ai.GoogleGenerativeAI(this.API_KEY).getGenerativeModel({ model: this.modelName, generationConfig });
  }
  changeSafetySettings(input) {
    this.safetySettings = input;
    this.currentModel = new import_generative_ai.GoogleGenerativeAI(this.API_KEY).getGenerativeModel({ model: this.modelName, safetySettings: this.safetySettings });
  }
  async loadSavedCredentialsIfExist(token_path) {
    try {
      const content = (await import_fs.default.promises.readFile(token_path)).toLocaleString();
      const credentials = JSON.parse(content);
      return import_googleapis.google.auth.fromJSON(credentials);
    } catch (err) {
      return null;
    }
  }
  async connectServiceAccount(clientKey, keyFile, privateKey, scopes) {
    this.jwtClient = new import_googleapis.google.auth.JWT(
      clientKey,
      keyFile,
      privateKey,
      scopes
    );
    await this.jwtClient.authorize();
  }
  async saveCredentials(client, credentials_path, token_path) {
    const content = (await import_fs.default.promises.readFile(credentials_path)).toLocaleString();
    const keys = JSON.parse(content);
    const key = keys.installed || keys.web;
    const payload = JSON.stringify({
      type: "authorized_user",
      client_id: key.client_id,
      client_secret: key.client_secret,
      refresh_token: client.credentials.refresh_token
    });
    await import_fs.default.promises.writeFile(token_path, payload);
  }
  async connectAuthClient(token_path, credentials_path, scopes) {
    this.authConnect = await this.loadSavedCredentialsIfExist(token_path);
    if (this.authConnect) {
      this.gmail = import_googleapis.google.gmail({ version: "v1", auth: this.authConnect });
      return;
    }
    this.authConnect = await (0, import_local_auth.authenticate)({
      scopes,
      keyfilePath: credentials_path
    });
    if (this.authConnect.credentials) {
      this.gmail = import_googleapis.google.gmail({ version: "v1", auth: this.authConnect });
      await this.saveCredentials(this.authConnect, credentials_path, token_path);
    }
  }
  async listFiles(pageSize) {
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    const res = await drive.files.list({
      pageSize,
      fields: "nextPageToken, files(id, name)"
    });
    const files = res.data.files;
    if (files?.length === 0) {
      console.log("No files Found!");
      return process.exit();
    }
    var list = [];
    files?.map((file) => {
      list.push([file.name, file.id]);
    });
    return list;
  }
  async uploadFile(fileName) {
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    if (fileName) {
      const file = await drive.files.create({
        media: {
          body: import_fs.default.createReadStream(fileName)
        },
        fields: "id",
        requestBody: {
          name: import_path.default.basename(fileName)
        }
      });
      return file.data.id;
    } else
      return "Please specify a file Name";
  }
  async updateFile(fileName, fileId) {
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    if (fileName && fileId) {
      const file = await drive.files.update({
        media: {
          body: import_fs.default.createReadStream(fileName)
        },
        fileId
      });
      return file.data.id;
    } else
      return "Please specify file name/file id";
  }
  async downloadFile(fileName, fileId, listenerFunction) {
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    if (fileName && fileId) {
      let dest = import_fs.default.createWriteStream(fileName);
      drive.files.get(
        { fileId, alt: "media" },
        { responseType: "stream" },
        (err, response) => {
          if (err) {
            console.log(err);
            return;
          }
          response?.data.on("end", listenerFunction).on("error", (err2) => {
            console.log(err2);
            return process.exit();
          }).pipe(dest);
        }
      );
    } else
      return "Please specify file name/file id";
  }
  async driveAndPrompt(prompt, fileName, fileId) {
    if (this.modelName === "google-pro") {
      return "Use Gemini Pro Vision";
    }
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    if (fileName && fileId) {
      return new Promise(
        (resolve, reject) => {
          const chunks = [];
          drive.files.get(
            { fileId, alt: "media" },
            { responseType: "stream" },
            (err, response) => {
              if (err) {
                console.log(err);
                return process.exit();
              }
              response?.data.on("data", (chunk) => chunks.push(Buffer.from(chunk))).on("error", () => {
                console.log(err);
                reject(process.exit());
              }).on("end", async () => {
                let imagePart = [{
                  inlineData: {
                    data: Buffer.concat(chunks).toString("base64"),
                    mimeType: "image/png"
                  }
                }];
                const result = await this.currentModel.generateContent([prompt, ...imagePart]);
                resolve(result.response.text());
              });
            }
          );
        }
      );
    } else
      return "Please specify file name/file id";
  }
  async returnImageBuffer(fileName, fileId) {
    const drive = import_googleapis.google.drive({ version: "v3", auth: this.jwtClient });
    if (fileName && fileId) {
      return new Promise((resolve, reject) => {
        const chunks = [];
        drive.files.get(
          { fileId, alt: "media" },
          { responseType: "stream" },
          (err, response) => {
            if (err) {
              reject(err);
            }
            response?.data.on("data", (chunk) => chunks.push(Buffer.from(chunk))).on("error", () => {
              resolve("Error");
            }).on("end", () => {
              resolve(Buffer.concat(chunks).toString("base64"));
            });
          }
        );
      });
    } else
      return "Please specify file name/file id";
  }
  async returnSnippet(userId, maxResult) {
    if (this.gmail === void 0) {
      console.log("Mail is Not defined");
      return process.exit();
    }
    const mails = (await this.gmail.users.messages.list({ userId, maxResults: maxResult }))?.data?.messages;
    if (mails !== void 0) {
      var listSnippets = [];
      for (const message of mails) {
        const msg = this.gmail.users.messages.get({
          userId,
          id: message.id
        });
        listSnippets.push((await msg).data.snippet);
      }
      return listSnippets;
    } else {
      return [];
    }
  }
  async promptSnippet(userId, snippetID, prompt) {
    if (this.gmail === void 0) {
      console.log("Mail is Not defined");
      return process.exit();
    }
    if (this.modelName === "gemini-pro-vision") {
      console.log("Use Gemini Pro");
      return process.exit();
    }
    return new Promise((resolve, reject) => {
      const msg = this.gmail.users.messages.get({
        userId,
        id: snippetID
      }).then(async (result) => {
        const output = this.currentModel.generateContent([prompt, result.data.snippet]);
        resolve((await output).response.text());
      }).catch((err) => {
        console.log(err);
        reject(err);
      });
    });
  }
  async sendMail(from, to, subject, text, html, name, prompt) {
    if (this.modelName === "gemini-pro-vision") {
      return "Use gemini-pro";
    }
    const oAuth2Client = new import_googleapis.google.auth.OAuth2(
      this.authConnect._clientId,
      this.authConnect._clientSecret,
      this.authConnect.redirectUri
    );
    oAuth2Client.setCredentials({ refresh_token: this.authConnect._refreshToken });
    try {
      const accessToken = await oAuth2Client.getAccessToken();
      const transport = nodemailer.createTransport({
        service: "gmail",
        auth: {
          type: "OAuth2",
          user: "yours authorised email address",
          clientId: this.authConnect._clientId,
          clientSecret: this.authConnect._clientSecret,
          refreshToken: this.authConnect._refreshToken,
          accessToken
        }
      });
      const result = await this.currentModel.generateContent([prompt, text]);
      const data = result.response.text();
      const mailOptions = {
        from: `${name} <${from}>`,
        to: `${to}`,
        subject: `${subject}`,
        text: `${data}`,
        html: `${html}`
      };
      console.log(mailOptions);
      const output = await transport.sendMail(mailOptions);
      return output;
    } catch (error) {
      return error;
    }
  }
  async translateText(text, targetLang) {
    let [translations] = await this.translate.translate(text, targetLang);
    return translations;
  }
  async getBlogData(API_KEY, bloggerID) {
    this.bloggerAPIKey = API_KEY;
    const result = await import_axios.default.get(`https://www.googleapis.com/blogger/v3/blogs/${bloggerID}?key=${API_KEY}`);
    return result.data;
  }
  async generateBlogContent(prompt) {
    const result = await this.currentModel.generateContent(prompt);
    return result.response.text();
  }
  async getBlogContent(API_KEY, bloggerID, doPrompt, prompt) {
    this.bloggerAPIKey = API_KEY;
    const result = await import_axios.default.get(`https://www.googleapis.com/blogger/v3/blogs/${bloggerID}/posts?key=${API_KEY}`);
    if (!doPrompt) {
      return result.data;
    }
    var list = result.data.items.map((element) => element.content);
    const output = await this.currentModel.generateContent([prompt, ...list]);
    return output.response.text();
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  NodeGem
});
